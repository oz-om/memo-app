//console
import eruda from "eruda";

eruda.init({
  tool: ["console", "elements", "snippets", "network", "resources", "sources"],
  defaults: {
    displaySize: 50,
    transparency: 0.9,
    theme: "Monokai Pro",
  },
});

// eruda.show();
